# 贡献说明

请先阅读 README 中的使用许可与发布边界。问题反馈请提供版本、预期行为、实际行为和可复现步骤，使用虚构药盒数据，不上传个人药品记录、照片、OPENID 或凭证。

修改代码前运行 `npm ci`，默认使用隔离演示模式。提交前运行 `npm run check`，说明测试范围；没有真机或开发工具条件时，明确标记未执行的项目。

业务规则变化应补充对应边界测试。不要提交本地配置、构建输出、原始日志、截图或账号数据，也不要在自动化中连接正式云环境。
